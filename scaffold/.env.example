# WordPress PHPUnit Docker — DB credentials
# Copy this file to .env and adjust the values if needed.
# Defaults match the docker-compose.yml service configuration.
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress
